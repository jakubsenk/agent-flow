{
  "task": "T10",
  "file": "skills/metrics/SKILL.md",
  "status": "done",
  "changes": [
    "Removed --format json flag from argument-hint and flag-parsing section",
    "Renamed Step 6 heuristic section to clarify it is fallback; added Step 6a for state.json-read with MEASURED/ESTIMATED/HYBRID classification",
    "Updated Step 7 report template: dual-mode per-pipeline line items (measured + estimated as separate rows), period summary table with separate rows (no mixed grand total), provenance footer listing estimated pipeline IDs and reason"
  ],
  "verification": {
    "grep_measured_estimated": "PASS — 18 matches",
    "grep_pipeline_total_tokens_state_json": "PASS — 10 matches",
    "grep_format_json_absent": "PASS — 0 matches"
  },
  "requirements_addressed": ["COST-R7", "COST-R8", "COST-R11"],
  "gate1_constraints_respected": [
    "No --format json flag added",
    "Aggregation window default (30 days) unchanged",
    "Existing heuristic tables preserved as fallback in Step 6"
  ]
}
