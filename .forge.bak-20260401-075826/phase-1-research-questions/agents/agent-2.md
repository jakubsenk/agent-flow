# Agent 2 — Research Questions: Areas 6–10

Generated by: Test Architecture Researcher
Date: 2026-03-31
Scope: Config contract completeness, cross-reference integrity, mock project coverage, deployment-verifier gap, feature pipeline gap

---

### Q6.1: Root cause iterations key missing from config-reader.md
**Area:** 6
**Files:** CLAUDE.md (Config Contract section), core/config-reader.md (Step 3, Retry Limits row)
**Question:** CLAUDE.md lists `Root cause iterations` (default: 3) as a Retry Limits key, and commands/fix-ticket.md and commands/fix-bugs.md reference it via `{Root cause iterations from config}`. However, `core/config-reader.md` only maps `retry.fixer_iterations`, `retry.test_attempts`, `retry.build_retries`, and `retry.spec_iterations` — `retry.root_cause_iterations` is entirely absent from the config-reader contract. Does this omission mean code-analyst always falls back to its hardcoded default of 3, and is there any test that would catch a drift between the config contract table in CLAUDE.md and the parsing specification in config-reader.md?

---

### Q6.2: Verify command declared optional-within-required but no test enforces its presence
**Area:** 6
**Files:** CLAUDE.md (Config Contract → Build & Test row), core/config-reader.md (Step 2, PR Description Template), core/fix-verification.md
**Question:** The Config Contract table marks `Build & Test` as required but lists `Verify command` parenthetically as "(optional — runs after PR merge)". The config-reader maps `build.verify_command` under the required `### Build & Test` section. Is there a test that validates the parser correctly treats a missing `Verify command` row as `null` rather than an error, and that `core/fix-verification.md` gracefully no-ops when `build.verify_command` is null?

---

### Q6.3: PR Description Template section boundary parsing ambiguity
**Area:** 6
**Files:** CLAUDE.md (Config Contract → PR Description Template), core/config-reader.md (Step 2)
**Question:** The config-reader spec says PR Description Template is "verbatim multi-line text under the subsection heading", which implies it reads everything from `### PR Description Template` to the next `###` heading. The mock project wraps this in triple-backtick fences. Does the config-reader contract specify how to handle the backtick fences — should they be stripped or included in the template string — and is there a test that validates the parsed template matches the raw multi-line content?

---

### Q6.4: Feature Workflow "On start set" fallback chain is undocumented in config-reader
**Area:** 6
**Files:** CLAUDE.md (Config Contract → optional Feature Workflow section), core/config-reader.md (Step 3), commands/implement-feature.md (Step 1)
**Question:** `implement-feature.md` Step 1 states "Set the state per Feature Workflow → On start set (fallback: Issue Tracker → On start set)". The `config-reader.md` maps `feature.on_start_set` as a key, but the fallback resolution logic (if `feature.on_start_set` is absent, use `issue_tracker.on_start_set`) is not documented in config-reader's output contract. Should config-reader itself perform this fallback and expose a merged field, or is each command responsible for implementing it independently? Which approach currently applies?

---

### Q7.1: core-include-refs.sh enforces 11 core files but lists only 10
**Area:** 7
**Files:** tests/scenarios/core-include-refs.sh (CORE_FILES array, line 16), core/ (11 .md files via Glob)
**Question:** `core-include-refs.sh` declares an array of 10 core files to validate (`CORE_FILES=(config-reader mcp-preflight fixer-reviewer-loop block-handler agent-override-injector decomposition-heuristics profile-parser post-publish-hook fix-verification state-manager)`) but CLAUDE.md states there are 11 core contracts. A Glob of `core/*.md` returns 11 files, with `mcp-detection.md` as the 11th. Is `mcp-detection.md` intentionally excluded from the existence and section checks in core-include-refs.sh, or is this an omission that leaves the newest core file unvalidated?

---

### Q7.2: frontmatter-completeness.sh and model-assignment.sh hardcode 18 agents — deployment-verifier is missing
**Area:** 7
**Files:** tests/scenarios/frontmatter-completeness.sh (AGENTS array, line 11), tests/scenarios/model-assignment.sh (SONNET_AGENTS array), agents/deployment-verifier.md
**Question:** Both `frontmatter-completeness.sh` and `model-assignment.sh` hardcode agent lists of 18 agents and their comment headers say "All 18 agents". The actual count per CLAUDE.md is 19 (deployment-verifier was added in v5.3.0 as the 19th agent). `deployment-verifier.md` exists and uses `model: sonnet`. Do these tests currently pass despite the wrong count annotation, and would adding a 20th agent in future break them silently, since both scripts use a hardcoded array rather than discovering agent files from disk?

---

### Q7.3: implement-feature.md references core/decomposition-heuristics.md but that contract is designed for fix-ticket (bug pipeline)
**Area:** 7
**Files:** commands/implement-feature.md (Step 5), core/decomposition-heuristics.md (Input Contract)
**Question:** `implement-feature.md` Step 5 says "Follow `core/decomposition-heuristics.md`" for task-tree cycle detection and AC coverage check. However, `core/decomposition-heuristics.md`'s Input Contract expects `code_analyst_output` fields (`risk`, `affected_files`, `estimated_diff_lines`, `independent_changes`), which are outputs of the code-analyst agent — an agent that does not exist in the feature pipeline. The feature pipeline uses spec-analyst and architect instead. Does implement-feature.md actually pass code-analyst output fields to decomposition-heuristics, or does it invoke a different part of that contract? Is the `FORCE/DISABLED/AUTO` flag-parsing section the only part of decomposition-heuristics that implement-feature actually uses?

---

### Q7.4: profile-parser.md valid stage set does not include all skippable stages listed in CLAUDE.md
**Area:** 7
**Files:** core/profile-parser.md (Step 6 valid set), CLAUDE.md (Pipeline Profiles → Stage names for skip)
**Question:** `profile-parser.md` Step 6 validates stage names against: `triage`, `code-analyst`, `spec-analyst`, `test-engineer`, `e2e-test-engineer`, `reproducer`, `browser-verifier`. CLAUDE.md's Pipeline Profiles config description lists the same set. However, `implement-feature.md`'s stage mapping notes that `code-analyst` and `triage` are N/A for the feature pipeline. If a consuming project's profile includes `triage` in skip_stages and uses it with `implement-feature`, does profile-parser.md silently accept the invalid stage (since it's in the valid set), and what behavior results when implement-feature ignores it?

---

### Q8.1: Mock project is missing 9 of 16 optional config sections
**Area:** 8
**Files:** tests/mock-project/CLAUDE.md, CLAUDE.md (Config Contract → Optional sections table)
**Question:** The mock project CLAUDE.md includes only 6 optional sections (Retry Limits, Hooks, Worktrees, Feature Workflow, Decomposition, Pipeline Profiles, Metrics — actually 7). It is missing: Module Docs, Custom Agents, Notifications, E2E Test, Browser Verification, Error Handling, Extra labels, Agent Overrides, and Local Deployment. This means no existing test exercises config-reader parsing of these 9 sections. Should the mock project include at least one representative optional section from each major feature area (notifications, browser verification, deployment) so that config-reader parsing and command integration tests can cover the optional paths?

---

### Q8.2: Mock project Retry Limits section is missing Spec iterations and Root cause iterations
**Area:** 8
**Files:** tests/mock-project/CLAUDE.md (Retry Limits section, lines 53–58), CLAUDE.md (Config Contract → Retry Limits row), core/config-reader.md (Retry Limits mapping)
**Question:** The mock project's Retry Limits table has only 3 keys: Fixer iterations, Test attempts, Build retries. It is missing `Spec iterations` (used by spec-analyst in the feature pipeline) and `Root cause iterations` (used by code-analyst in the bug pipeline). Since tests that reference `implement-feature` or code-analyst behavior use this mock project as their config source, will those agents always fall back to defaults, potentially masking a bug where a misconfigured (or zero) value for those keys is passed through without validation?

---

### Q9.1: deployment-verifier verdict set covers 5 states but check-deploy Step 2 only handles PORT_CONFLICT early exit
**Area:** 9
**Files:** agents/deployment-verifier.md (Step 9, verdict list), commands/check-deploy.md (Step 2, action = start branch)
**Question:** The deployment-verifier defines 5 verdicts: HEALTHY, UNHEALTHY, PORT_CONFLICT, START_FAILED, SKIPPED. `check-deploy.md` Step 2 handles `action = start` by checking port conflicts before dispatching the agent, but the command's Step 4 ("Display the deployment-verifier agent's report verbatim") and Step 5 (state update) do not define any conditional logic based on the returned verdict. Should there be a test that validates: (a) each verdict produces a distinct state.json `deployment.verdict` value, and (b) that UNHEALTHY and START_FAILED surface a non-zero exit signal to the user rather than silently completing with `status: completed`?

---

### Q9.2: deployment-verifier secret redaction applies only to docker logs but not to native process output
**Area:** 9
**Files:** agents/deployment-verifier.md (Step 7, redaction rule; Step 4, native start)
**Question:** The agent's Step 7 defines log redaction rules (PASSWORD=, TOKEN=, SECRET=, API_KEY=, PRIVATE_KEY=, Authorization:) specifically within "Docker inspection" — the step that only runs when `Type = docker`. Step 4 (native start) captures process output via Bash but has no equivalent redaction rule. Should there be a property test validating that secret redaction is enforced for native-type deployments as well, or is the current scope intentionally docker-only? If intentionally docker-only, should this be documented explicitly in the Constraints section?

---

### Q9.3: check-deploy port validation is duplicated between command and agent — no test checks consistency
**Area:** 9
**Files:** commands/check-deploy.md (Step 1, port validation logic), agents/deployment-verifier.md (Step 2, port validation)
**Question:** Both `check-deploy.md` Step 1 and `deployment-verifier.md` Step 2 independently implement the same port validation rule: "digits only, range 1–65535". This duplication means a future change to the validation rule in one file may not be reflected in the other. Is there a test that would catch a discrepancy between the command-level and agent-level port validation rules (e.g., one accepting port 0 and the other rejecting it)?

---

### Q10.1: Feature pipeline acceptance gate skip condition contradicts CLAUDE.md pipeline diagram
**Area:** 10
**Files:** commands/implement-feature.md (Step 6g), CLAUDE.md (Feature Pipeline diagram: "Acceptance gate (always)")
**Question:** CLAUDE.md's Feature Pipeline diagram shows `[Acceptance gate (always)]`, implying it runs for every feature including single-pass. However, `implement-feature.md` Step 6g explicitly states "In single-pass mode (no decomposition), this step is skipped." These are contradictory. Which is the authoritative source? Should there be a test that validates the acceptance gate is invoked (or explicitly skipped) in the single-pass code path, and that the state.json field `acceptance_gate.status` is set to `"skipped"` rather than simply absent?

---

### Q10.2: Feature pipeline state.json field reuse (triage.status for spec-analyst) is undocumented in schema
**Area:** 10
**Files:** commands/implement-feature.md (Step 3, state.json note), state/schema.md
**Question:** `implement-feature.md` Step 3 says "Update state.json: set `triage.status` to 'completed' (field reused for spec-analyst AC)". This is an explicit field reuse — the bug-pipeline's `triage` object is repurposed to hold spec-analyst output in the feature pipeline. Is this reuse documented in `state/schema.md`? If the schema treats `triage` as bug-only, could a `resume-ticket.md` heuristic incorrectly resume a feature pipeline at the wrong step because it found a completed `triage` status?

---

### Q10.3: --description flag duplicate-check uses first 50 characters — no test validates the matching threshold
**Area:** 10
**Files:** commands/implement-feature.md (Step 0c, duplicate check), tests/scenarios/ (no match for implement-feature duplicate-check)
**Question:** The `--description` flag flow in Step 0c specifies: "Search the tracker for existing issues whose title starts with the same first 50 characters as the extracted title." This is a concrete algorithmic property (50-char prefix match, skipped in YOLO mode). Zero test scenarios cover this flow. Should there be a structural test validating that: (a) the 50-character threshold is documented and applied consistently, (b) YOLO mode bypass is explicitly stated, and (c) the user confirmation prompt text matches the Block Comment Template format?

---

### Q10.4: Decomposition task-tree file path (.claude/decomposition/) is not in state schema and collides with Claude Code internals
**Area:** 10
**Files:** commands/implement-feature.md (Step 5, "Save task tree" line), state/schema.md
**Question:** `implement-feature.md` Step 5 writes the decomposition task tree to `.claude/decomposition/{ISSUE-ID}.yaml`. The `.claude/` directory is Claude Code's own state directory (settings.local.json, memory files, etc.). Writing plugin artifacts into `.claude/decomposition/` could conflict with Claude Code's own directory management. Is this path intentional and safe, and is there a test that validates the task tree file is written to the correct path and cleaned up (or excluded from git) after pipeline completion?
