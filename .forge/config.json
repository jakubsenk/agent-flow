{
  "pipeline_mode": "adaptive",
  "pipeline_mode__provenance": { "source": "default", "reason": "Adaptive mode suitable for cross-cutting migration with many parallel subtasks" },
  "jit": {
    "enabled": true,
    "enabled__provenance": { "source": "meta-agent", "reason": "Composite complexity 5 — JIT refinement recommended for phase-specific prompt adaptation" },
    "timeout": 120,
    "fallback_timeout": 60,
    "max_consecutive_fallbacks": 2
  },
  "replanning": {
    "enabled": true,
    "max_cycles": 1,
    "max_cycles__provenance": { "source": "default", "reason": "Ambiguity score 1 — task is fully specified, single replanning cycle sufficient" },
    "divergence_threshold": 0.3,
    "divergence_threshold__provenance": { "source": "default", "reason": "Standard threshold appropriate for fully-specified migration task" },
    "trigger_classes": ["PIVOTED"]
  },
  "fast_track": {
    "enabled": false,
    "enabled__provenance": { "source": "meta-agent", "reason": "Composite complexity 5 exceeds fast-track threshold of 2 — full pipeline required" }
  },
  "meta_agent": {
    "confidence_threshold": 0.7
  },
  "verification": {
    "dimension_weights": {
      "security": 0.2,
      "correctness": 0.4,
      "spec_alignment": 0.3,
      "robustness": 0.1
    },
    "dimension_weights__provenance": { "source": "meta-agent", "reason": "Migration correctness is critical (every rename must be caught); spec alignment important to verify all user-specified changes were applied" }
  },
  "skip_phases": [],
  "approval_gates": [3, 4, 6],
  "tdd": {
    "mutation_threshold": 70
  },
  "review": {
    "triage_enabled": false
  },
  "oracle": {
    "enabled": true
  }
}
