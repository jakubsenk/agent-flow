# Phase 1 — Research Questions

You are a **Senior Technical Researcher** analyzing a codebase to prepare for a well-defined feature implementation.

## Task Context

We are adding two features to the `agents/scaffolder.md` file in a Claude Code plugin (`ceos-agents`):

1. **E2E Test Generation** — When stack includes a web framework + Playwright, scaffolder generates a basic e2e test suite (config, smoke test, test script).
2. **Application Documentation for Agents** — Scaffolder generates `docs/ARCHITECTURE.md` and populates `Module Docs | Path` in CLAUDE.md Automation Config.

This is a **pure markdown plugin** — no build system, no runtime code. The scaffolder agent is a markdown file with YAML frontmatter that instructs Claude Code how to scaffold projects.

## Research Questions

Answer each question thoroughly with file paths and exact quotes where relevant.

### Q1: Scaffolder Agent Structure
Read `agents/scaffolder.md` and document:
- The complete batch numbering scheme (Batch 1 through Batch 6)
- All 9 scorecard items and their exact format
- All constraints (NEVER rules)
- The file count target constraint
- How conditional batches work (Batch 6 pattern — what triggers inclusion/exclusion?)

### Q2: Scaffold Skill Pipeline
Read `skills/scaffold/SKILL.md` and document:
- How the scaffolder agent is invoked (what context is passed?)
- Where does validation happen (Step 3)?
- Where is CLAUDE.md auto-filled (Step 4a)?
- Is there any Module Docs handling currently?
- What would need to change in the skill to support the new features?

### Q3: Module Docs Consumption
Search for `Module Docs` across all agent and skill files. Document:
- Which agents read Module Docs and how?
- What is the expected format of the Module Docs path?
- How would pointing Module Docs at scaffold-generated docs work with zero agent changes?

### Q4: E2E Test Config Section
Read the CLAUDE.md config contract for the `E2E Test` optional section. Document:
- What keys does it have?
- When is it generated by scaffolder? (scaffold v2 mode constraint)
- How does it relate to e2e-test-engineer?

### Q5: Existing Test Patterns
Read 2-3 test scenarios from `tests/scenarios/` that validate scaffolder behavior. Document:
- What do scaffold tests typically assert?
- What patterns should new tests follow?
- Where would assertions for "E2E Test Setup" scorecard and "docs/ARCHITECTURE.md" generation go?

### Q6: File Count and Target Limits
From `agents/scaffolder.md` constraints, document:
- Current file count targets (simple / DB+CI+Docker / web)
- How would adding e2e files (~3 files) and docs/ARCHITECTURE.md (~1 file) affect the target?
- Should the ceiling be raised?

## Output Format

For each question, provide:
```
### QN: [Title]
**Answer:** [Detailed answer with file paths and quotes]
**Implications for implementation:** [What this means for the feature work]
```
