# .agent-flow/

Runtime directory for agent-flow per-run pipeline state.

Populated automatically when the pipeline runs against this mock project. Do not commit pipeline artifacts from this directory.
