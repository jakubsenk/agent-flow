[project]
name = "mock-project"
version = "0.1.0"
description = "Mock project for agent-flow pipeline testing"
requires-python = ">=3.10"

[project.dependencies]
# empty — minimal fixture for v8-setup-agents-python.sh heuristic detection
