# Phase 4 — Specification

**SKIPPED** — Fast-track path. Phase 4 is not executed.

This phase would produce an EARS-format specification. In fast-track mode, the fix is scoped by the delta between implement-feature and fix-ticket. No formal spec required.
