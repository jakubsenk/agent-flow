# Scaffold.md Research Findings — Agent 1

Source file: `commands/scaffold.md`
Total lines: 567

---

## 1. Step 0: Mode Selection (Full Content)

**Line range: L51–L66**

```
### Step 0: Mode Selection

Generate `run_id` as `scaffold-{timestamp}` (format: `scaffold-YYYYMMDD-HHmmss`). Create `.ceos-agents/{run_id}/` directory. Initialize `state.json` following the schema in `state/schema.md` with `status: "running"`, `mode: "code-project"`, `pipeline: "scaffold"`, `run_id: "{run_id}"`, `parent_run_id: null`. Follow atomic write protocol from `core/state-manager.md`.

If `--no-implement`:
→ Skip to legacy flow: stack-selector → scaffolder → validate → move → git init → report (v3.x behavior, steps L1–L6 below). EXIT pipeline.

Display mode selection:
```
How do you want to proceed?
(a) Interactive — we'll build the spec together
(b) YOLO with checkpoint — I'll design everything, you approve before implementation
(c) Fully automated — I'll design and implement everything, no stops (YOLO mode)
```

Store selected mode.
```

**Exact lines:**
- L51: `### Step 0: Mode Selection`
- L52: *(blank)*
- L53: `Generate \`run_id\` as \`scaffold-{timestamp}\` (format: \`scaffold-YYYYMMDD-HHmmss\`). Create \`.ceos-agents/{run_id}/\` directory. Initialize \`state.json\` following the schema in \`state/schema.md\` with \`status: "running"\`, \`mode: "code-project"\`, \`pipeline: "scaffold"\`, \`run_id: "{run_id}"\`, \`parent_run_id: null\`. Follow atomic write protocol from \`core/state-manager.md\`.`
- L54: *(blank)*
- L55: `If \`--no-implement\`:`
- L56: `→ Skip to legacy flow: stack-selector → scaffolder → validate → move → git init → report (v3.x behavior, steps L1–L6 below). EXIT pipeline.`
- L57: *(blank)*
- L58: `Display mode selection:`
- L59: ` ``` `
- L60: `How do you want to proceed?`
- L61: `(a) Interactive — we'll build the spec together`
- L62: `(b) YOLO with checkpoint — I'll design everything, you approve before implementation`
- L63: `(c) Fully automated — I'll design and implement everything, no stops (YOLO mode)`
- L64: ` ``` `
- L65: *(blank)*
- L66: `Store selected mode.`

---

## 2. Step 4: Git Init (Full Content)

**Line range: L251–L261**

```
### Step 4: Git Init

```bash
git init
git add .
git commit -m "feat: initial project scaffold

Stack: {language} + {framework}
Spec: {N} epics, {M} user stories
Generated by ceos-agents /scaffold"
```
```

**Exact lines:**
- L251: `### Step 4: Git Init`
- L252: *(blank)*
- L253: ` ```bash `
- L254: `git init`
- L255: `git add .`
- L256: `git commit -m "feat: initial project scaffold`
- L257: *(blank)*
- L258: `Stack: {language} + {framework}`
- L259: `Spec: {N} epics, {M} user stories`
- L260: `Generated by ceos-agents /scaffold"`
- L261: ` ``` `

---

## 3. Step 4b: Tracker Configuration (Full Content)

**Line range: L263–L298**

```
### Step 4b: Tracker Configuration (Auto-Finalize) — Full YOLO skips this step

If mode is Full YOLO → skip to Step 4c (TODOs remain — cannot guess tracker URLs in unattended mode).

Check the generated CLAUDE.md for TODO markers (`<!-- TODO:`):

1. Scan all `| Key | Value |` rows in `## Automation Config` for values containing `<!-- TODO:`.
2. Collect incomplete keys into a list: `incomplete_keys[]`.

If `incomplete_keys` is empty → skip to Step 4c (no TODOs to fill).

Display:
```
Your project needs a few configuration values before the pipeline can run.
I'll ask about each one now. Press Enter to skip any value.
```

For each incomplete key, prompt the user:

**Issue Tracker keys:**
- `Type` → "Issue tracker type? [youtrack/github/jira/linear/gitea/redmine]" (default from spec if available)
- `Instance` → "What is your {Type} instance URL?" (show format example)
- `Project` → "What is your project key/name in {Type}?"
- `Bug query` → Auto-fill from tracker type defaults. Display: "I'll use this bug query: `{query}`. Change? [Enter to keep / type new]"
- `State transitions` → Auto-fill from tracker type defaults. Display and confirm.
- `On start set` → Auto-fill from tracker type defaults.

**Source Control keys:**
- `Remote` → "What is your repository remote? (e.g., owner/repo)"
- `Base branch` → "Base branch? [main]"
- `Branch naming` → Show default pattern, confirm.

After collecting values:
1. Write values into CLAUDE.md, replacing `<!-- TODO: ... -->` markers with user-provided values using the Edit tool
2. Re-validate CLAUDE.md (scan for remaining TODOs)
3. Commit the config changes: `git add CLAUDE.md && git commit -m "chore: configure Automation Config"`
```

**Exact lines:**
- L263: `### Step 4b: Tracker Configuration (Auto-Finalize) — Full YOLO skips this step`
- L264: *(blank)*
- L265: `If mode is Full YOLO → skip to Step 4c (TODOs remain — cannot guess tracker URLs in unattended mode).`
- L266: *(blank)*
- L267: `Check the generated CLAUDE.md for TODO markers (\`<!-- TODO:\`)`:`
- L268: *(blank)*
- L269: `1. Scan all \`| Key | Value |\` rows in \`## Automation Config\` for values containing \`<!-- TODO:\`.`
- L270: `2. Collect incomplete keys into a list: \`incomplete_keys[]\`.`
- L271: *(blank)*
- L272: `If \`incomplete_keys\` is empty → skip to Step 4c (no TODOs to fill).`
- L273: *(blank)*
- L274: `Display:`
- L275: ` ``` `
- L276: `Your project needs a few configuration values before the pipeline can run.`
- L277: `I'll ask about each one now. Press Enter to skip any value.`
- L278: ` ``` `
- L279: *(blank)*
- L280: `For each incomplete key, prompt the user:`
- L281: *(blank)*
- L282: `**Issue Tracker keys:**`
- L283: `- \`Type\` → "Issue tracker type? [youtrack/github/jira/linear/gitea/redmine]" (default from spec if available)`
- L284: `- \`Instance\` → "What is your {Type} instance URL?" (show format example)`
- L285: `- \`Project\` → "What is your project key/name in {Type}?"`
- L286: `- \`Bug query\` → Auto-fill from tracker type defaults. Display: "I'll use this bug query: \`{query}\`. Change? [Enter to keep / type new]"`
- L287: `- \`State transitions\` → Auto-fill from tracker type defaults. Display and confirm.`
- L288: `- \`On start set\` → Auto-fill from tracker type defaults.`
- L289: *(blank)*
- L290: `**Source Control keys:**`
- L291: `- \`Remote\` → "What is your repository remote? (e.g., owner/repo)"`
- L292: `- \`Base branch\` → "Base branch? [main]"`
- L293: `- \`Branch naming\` → Show default pattern, confirm.`
- L294: *(blank)*
- L295: `After collecting values:`
- L296: `1. Write values into CLAUDE.md, replacing \`<!-- TODO: ... -->\` markers with user-provided values using the Edit tool`
- L297: `2. Re-validate CLAUDE.md (scan for remaining TODOs)`
- L298: `3. Commit the config changes: \`git add CLAUDE.md && git commit -m "chore: configure Automation Config"\``

---

## 4. Step 4c: MCP Guidance (Full Content)

**Line range: L300–L307**

```
### Step 4c: MCP Guidance

If Issue Tracker Instance was filled in Step 4b:
- Display: "To connect to {Type} at {Instance}, configure an MCP server. Run `/ceos-agents:init` to set it up."

If Issue Tracker Instance was NOT filled (TODOs remain): skip this step.

This is informational only — scaffold does NOT block on MCP availability.
```

**Exact lines:**
- L300: `### Step 4c: MCP Guidance`
- L301: *(blank)*
- L302: `If Issue Tracker Instance was filled in Step 4b:`
- L303: `- Display: "To connect to {Type} at {Instance}, configure an MCP server. Run \`/ceos-agents:init\` to set it up."`
- L304: *(blank)*
- L305: `If Issue Tracker Instance was NOT filled (TODOs remain): skip this step.`
- L306: *(blank)*
- L307: `This is informational only — scaffold does NOT block on MCP availability.`

---

## 5. Step 9: Issue Tracker Optional (Full Content)

**Line range: L481–L501**

```
### Step 9: Issue Tracker (Optional)

Check if Issue Tracker section in generated CLAUDE.md has TODO markers
  (look for `<!-- TODO:` in Instance or Project values).
If TODO markers present → skip (no tracker configured).

If tracker configured and mode is not Full YOLO:
  "Create cards in issue tracker for implemented features? [Y/n]"

  If yes:
    For each spec/epics/*.md:
      Create epic card in tracker (summary from epic title, description from epic content)
      For each user story in epic:
        Create sub-issue under epic card
        Link back to spec file in repo
      Set status per State transitions from Automation Config

  If no → skip.

If mode is Full YOLO and tracker configured:
  Skip — do not create cards automatically in Full YOLO.
```

**Exact lines:**
- L481: `### Step 9: Issue Tracker (Optional)`
- L482: *(blank)*
- L483: `Check if Issue Tracker section in generated CLAUDE.md has TODO markers`
- L484: `  (look for \`<!-- TODO:\` in Instance or Project values).`
- L485: `If TODO markers present → skip (no tracker configured).`
- L486: *(blank)*
- L487: `If tracker configured and mode is not Full YOLO:`
- L488: `  "Create cards in issue tracker for implemented features? [Y/n]"`
- L489: *(blank)*
- L490: `  If yes:`
- L491: `    For each spec/epics/*.md:`
- L492: `      Create epic card in tracker (summary from epic title, description from epic content)`
- L493: `      For each user story in epic:`
- L494: `        Create sub-issue under epic card`
- L495: `        Link back to spec file in repo`
- L496: `      Set status per State transitions from Automation Config`
- L497: *(blank)*
- L498: `  If no → skip.`
- L499: *(blank)*
- L500: `If mode is Full YOLO and tracker configured:`
- L501: `  Skip — do not create cards automatically in Full YOLO.`

---

## 6. Step 10: Final Report (Full Content)

**Line range: L503–L538**

```
### Step 10: Final Report

Update `state.json`: set top-level `status` to `"completed"`. Follow atomic write protocol from `core/state-manager.md`.

Display:
```
## Scaffold Complete

**Project:** {name from spec/README.md Vision section, or project description}
**Mode:** {Interactive | YOLO with checkpoint | Full YOLO}
**Stack:** {from spec/README.md Tech Stack}
**Spec:** {N} iterations, {APPROVED | approved with warnings}
**Features:** {implemented} / {total} ({blocked} blocked)
**Tests:** {unit count} unit, {integration count} integration, {e2e count} e2e
**Commits:** {count}

### Generated files: {count}
### Spec: spec/
### Blocked features (if any):
- {subtask title} — {block reason}

### Remaining TODOs in CLAUDE.md:
- [ ] Issue Tracker instance
- [ ] Source Control remote

### Next steps:
{if TODOs remain in CLAUDE.md}
1. Complete configuration: run `/ceos-agents:onboard --update`
2. Run `/ceos-agents:check-setup` to validate configuration
3. Run `/ceos-agents:scaffold-validate` to verify project state

{if all config is complete}
1. Your project is ready. Try `/ceos-agents:implement-feature` with a tracker issue.
2. Run `/ceos-agents:check-setup` to validate configuration
3. Run `/ceos-agents:scaffold-validate` to verify project state
```
```

**Exact lines:**
- L503: `### Step 10: Final Report`
- L504: *(blank)*
- L505: `Update \`state.json\`: set top-level \`status\` to \`"completed"\`. Follow atomic write protocol from \`core/state-manager.md\`.`
- L506: *(blank)*
- L507: `Display:`
- L508: ` ``` `
- L509: `## Scaffold Complete`
- L510: *(blank)*
- L511: `**Project:** {name from spec/README.md Vision section, or project description}`
- L512: `**Mode:** {Interactive | YOLO with checkpoint | Full YOLO}`
- L513: `**Stack:** {from spec/README.md Tech Stack}`
- L514: `**Spec:** {N} iterations, {APPROVED | approved with warnings}`
- L515: `**Features:** {implemented} / {total} ({blocked} blocked)`
- L516: `**Tests:** {unit count} unit, {integration count} integration, {e2e count} e2e`
- L517: `**Commits:** {count}`
- L518: *(blank)*
- L519: `### Generated files: {count}`
- L520: `### Spec: spec/`
- L521: `### Blocked features (if any):`
- L522: `- {subtask title} — {block reason}`
- L523: *(blank)*
- L524: `### Remaining TODOs in CLAUDE.md:`
- L525: `- [ ] Issue Tracker instance`
- L526: `- [ ] Source Control remote`
- L527: *(blank)*
- L528: `### Next steps:`
- L529: `{if TODOs remain in CLAUDE.md}`
- L530: `1. Complete configuration: run \`/ceos-agents:onboard --update\``
- L531: `2. Run \`/ceos-agents:check-setup\` to validate configuration`
- L532: `3. Run \`/ceos-agents:scaffold-validate\` to verify project state`
- L533: *(blank)*
- L534: `{if all config is complete}`
- L535: `1. Your project is ready. Try \`/ceos-agents:implement-feature\` with a tracker issue.`
- L536: `2. Run \`/ceos-agents:check-setup\` to validate configuration`
- L537: `3. Run \`/ceos-agents:scaffold-validate\` to verify project state`
- L538: ` ``` `

---

## 7. MCP Pre-flight Check Section (Full Content)

**Line range: L540–L551**

```
## MCP Pre-flight Check

MCP pre-flight check is only required when:
- `--issue` flag is used (Step 1 — reading issue description from tracker)
- Step 9 — creating cards in issue tracker (only when tracker is configured and user opts in)

For `--no-implement`, keep the same behavior as v3.x (MCP check before stack-selector).

Before any MCP operation, verify MCP tool availability:
- Read Type from Automation Config (Issue Tracker section)
- Check that at least one `mcp__*` tool matching the tracker type is accessible
- If not accessible → STOP with: "MCP server for {Type} is not available. Run `/ceos-agents:check-setup` for diagnostics or `/ceos-agents:init` to configure."
```

**Exact lines:**
- L540: `## MCP Pre-flight Check`
- L541: *(blank)*
- L542: `MCP pre-flight check is only required when:`
- L543: `- \`--issue\` flag is used (Step 1 — reading issue description from tracker)`
- L544: `- Step 9 — creating cards in issue tracker (only when tracker is configured and user opts in)`
- L545: *(blank)*
- L546: `For \`--no-implement\`, keep the same behavior as v3.x (MCP check before stack-selector).`
- L547: *(blank)*
- L548: `Before any MCP operation, verify MCP tool availability:`
- L549: `- Read Type from Automation Config (Issue Tracker section)`
- L550: `- Check that at least one \`mcp__*\` tool matching the tracker type is accessible`
- L551: `- If not accessible → STOP with: "MCP server for {Type} is not available. Run \`/ceos-agents:check-setup\` for diagnostics or \`/ceos-agents:init\` to configure."`

---

## 8. Rules Section (Full Content)

**Line range: L553–L567**

```
## Rules

- **Agent Overrides** from Agent Overrides section (if it exists): Follow `core/agent-override-injector.md`.
  - Path (default: `customization/`)
- Before dispatching any agent via Task tool, check if `{Agent Overrides path}/{agent-name}.md` exists.
  If yes, append its content to the agent's context as: "## Project-Specific Instructions\n{file content}".
- Always generate into a temp directory — move only after successful validation
- Never overwrite existing files without confirmation
- Scaffolder generates CLAUDE.md — but Issue Tracker instance and Source Control remote
  require manual completion (marked with TODO comments)
- If the user did not provide a project description → ask for one
- When running in v2 mode (not --no-implement), spec/ is the source of truth for all downstream agents
- Block comments in scaffold context go to stdout, not issue tracker
- Rollback-agent is called for fixer/reviewer/test-engineer blocks; for scaffolder blocks, command handles cleanup (delete temp dir)
```

**Exact lines:**
- L553: `## Rules`
- L554: *(blank)*
- L555: `- **Agent Overrides** from Agent Overrides section (if it exists): Follow \`core/agent-override-injector.md\`.`
- L556: `  - Path (default: \`customization/\`)`
- L557: `- Before dispatching any agent via Task tool, check if \`{Agent Overrides path}/{agent-name}.md\` exists.`
- L558: `  If yes, append its content to the agent's context as: "## Project-Specific Instructions\n{file content}".`
- L559: `- Always generate into a temp directory — move only after successful validation`
- L560: `- Never overwrite existing files without confirmation`
- L561: `- Scaffolder generates CLAUDE.md — but Issue Tracker instance and Source Control remote`
- L562: `  require manual completion (marked with TODO comments)`
- L563: `- If the user did not provide a project description → ask for one`
- L564: `- When running in v2 mode (not --no-implement), spec/ is the source of truth for all downstream agents`
- L565: `- Block comments in scaffold context go to stdout, not issue tracker`
- L566: `- Rollback-agent is called for fixer/reviewer/test-engineer blocks; for scaffolder blocks, command handles cleanup (delete temp dir)`

*(L567 is blank — end of file)*

---

## 9. State Detection Section (Full Content)

**Line range: L38–L47** (runs before Step 0)

```
## State Detection

Before starting, check the target directory:

1. **Empty directory** (or does not exist) → full scaffold
2. **Existing project without CLAUDE.md** → offer: "Project exists but has no CLAUDE.md. Do you want `/scaffold-add claude-md`?"
3. **Existing project with CLAUDE.md** → offer: "Project already has Automation Config. Do you want `/implement-feature`?"
4. **Existing git repo with uncommitted changes** → warn: "Uncommitted changes. Commit or stash them."

If state is not 1 and user does not confirm → stop.
```

**Exact lines:**
- L38: `## State Detection`
- L39: *(blank)*
- L40: `Before starting, check the target directory:`
- L41: *(blank)*
- L42: `1. **Empty directory** (or does not exist) → full scaffold`
- L43: `2. **Existing project without CLAUDE.md** → offer: "Project exists but has no CLAUDE.md. Do you want \`/scaffold-add claude-md\`?"`
- L44: `3. **Existing project with CLAUDE.md** → offer: "Project already has Automation Config. Do you want \`/implement-feature\`?"`
- L45: `4. **Existing git repo with uncommitted changes** → warn: "Uncommitted changes. Commit or stash them."`
- L46: *(blank)*
- L47: `If state is not 1 and user does not confirm → stop.`

---

## 10. --no-implement Legacy Flow Line Range

**Answer: L68–L147**

The `--no-implement` legacy flow (steps L1–L6) spans lines **68 to 147**.

Breakdown:
- L68: `### --no-implement Legacy Flow (v3.x)` (section header)
- L70–L76: `#### L1. Stack-selector`
- L78–L88: `#### L2. Scaffolder — generate into temp directory`
- L90–L103: `#### L3. Validation`
- L104–L114: `#### L4. Move to target directory`
- L116–L125: `#### L5. Git init`
- L127–L145: `#### L6. Report`
- L147: `END of --no-implement legacy flow.`

The entry point trigger is at L55–L56 (inside Step 0):
- L55: `If \`--no-implement\`:`
- L56: `→ Skip to legacy flow: stack-selector → scaffolder → validate → move → git init → report (v3.x behavior, steps L1–L6 below). EXIT pipeline.`

The separator `---` at L149 marks the end boundary of the legacy flow block.
